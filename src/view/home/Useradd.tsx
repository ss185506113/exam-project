import React, { Component } from 'react'

export default class Useradd extends Component {
    render() {
        return (
            <div>
                useradd
            </div>
        )
    }
}
